'use client'

// import dynamic from "next/dynamic";

// const MapObject = dynamic(() => import('@/components/OSM'), {
//   ssr: false,
// })

export default function Map() {
  return (
    <div>
      {/* <MapObject /> */}
    </div>
  )
}